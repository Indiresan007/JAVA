import java.util.*;
class Main {
    public static void main(String[] args)
    {
        Scanner sc=new Scanner(System.in);
        int num = sc.nextInt();
        int result = Integer.bitCount(num);
        System.out.println("Number of one-bits in num: "+ result);
    }
}
